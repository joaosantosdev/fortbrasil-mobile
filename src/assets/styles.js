import colors from './colors';

const styles = {
    link: {
        color: colors.primaryColor,
        fontWeight: 'bold',
        fontSize: 18,
    },
    shadow:{
        shadowColor: '#000',
        shadowOffset: {
            width: 0,
            height: 2,
        },
        shadowOpacity: 0.25,
        shadowRadius: 3.84,
        elevation: 5,
    }
};

export default styles;
